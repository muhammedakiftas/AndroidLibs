<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/toolbar"
    android:layout_width="match_parent"
    android:layout_height="?android:attr/actionBarSize"
    android:background="?attr/colorPrimary"
    app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
    app:layout_scrollFlags="scroll|enterAlways"
    app:layout_collapseMode="pin">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- This is a centered logo ?attr/actionBarSize -->

        <ImageView
            android:id="@+id/toolbar_logo"
            android:src="@drawable/hayalhanem_white"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginRight="@dimen/myActionBarSize"
            android:layout_marginLeft="@dimen/myActionBarSize"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="4dp"
            android:layout_gravity="center" />

        <!-- This is a centered title

        <TextView
            android:id="@+id/toolbar_title"
            android:orientation="horizontal"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginRight="?android:attr/actionBarSize"
            android:layout_gravity="center"
            android:gravity="center_vertical"
            android:visibility="gone"
            android:text="@string/app_name"
            android:textColor="@color/flatui_clouds"
            style="@style/TextAppearance.AppCompat.Widget.ActionBar.Title.Inverse"
            />
        -->

        <!-- This is a custom left side button -->

        <Button
            android:id="@+id/btn_settings"
            android:layout_width="?android:attr/actionBarSize"
            android:layout_height="?android:attr/actionBarSize"
            android:layout_marginRight="?android:attr/actionBarSize"
            android:layout_gravity="start|center_vertical"
            android:text="Bağış"
            android:drawableTop="@android:drawable/ic_menu_edit"
            style="@style/AppTheme.ActionButton" />


        <!-- This is a custom right side button -->

        <Button
            android:id="@+id/btn_search"
            android:layout_width="?android:attr/actionBarSize"
            android:layout_height="?android:attr/actionBarSize"
            android:layout_gravity="end"
            android:text="Bize Ulaşın"
            android:drawableTop="@android:drawable/ic_menu_call"
            style="@style/AppTheme.ActionButton" />

    </FrameLayout>
</android.support.v7.widget.Toolbar>


<!--
<RelativeLayout
            android:id="@+id/relative1"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center">

            <ImageView
                android:id="@+id/iv_action"
                android:layout_width="200dp"
                android:layout_height="match_parent"
                android:layout_centerHorizontal="true"
                android:src="@drawable/hayalhanem_white" />

            <TextView
                android:id="@+id/tv_iletisim"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_alignParentRight="true"
                android:layout_toRightOf="@+id/iv_action"
                android:gravity="center"
                android:text="Bize\nUlaşın"
                android:textColor="@color/flatui_silver"
                android:textSize="18sp" />

            <TextView
                android:id="@+id/tv_bagis"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_alignParentLeft="true"
                android:layout_toLeftOf="@+id/iv_action"
                android:gravity="center"
                android:text="Bağış"
                android:textColor="@color/flatui_silver"
                android:textSize="18sp" />
        </RelativeLayout>
-->